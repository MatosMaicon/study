apiVersion: v1
kind: Service
metadata:
  name: project-c-service
  namespace: application
spec:
  selector:
    app: project-c
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer
